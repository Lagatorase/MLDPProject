# MLDPProject
## Note on model file

v1.0
The trained model (`hdb_resale_model.pkl`) is not included in this repository due to file size limits on GitHub. The notebook contains the full training pipeline and can be re-run to regenerate the model. This is a known limitation currently being discussed with my lecturer.